# imap-sieve-filter

Simple tool to apply a sieve filter to an IMAP mailbox retroactively. It only supports what I need from it, namely `fileinto`.

Uses the `sieve-test` command to execute the sieve-script.
